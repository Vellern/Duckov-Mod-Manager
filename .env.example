# ==========================================
# Duckov Mod Manager - Electron App Configuration
# ==========================================

# Environment Mode
NODE_ENV=development

# ==========================================
# Electron Configuration
# ==========================================

# Vite Dev Server URL (for Electron development mode)
# The main process loads the React app from this URL in development
VITE_DEV_SERVER_URL=http://localhost:3001

# ==========================================
# Local Workshop Data Path
# ==========================================
# This is where your downloaded Workshop mod folders are stored
# Each mod should be in a folder named with its Steam Workshop ID
#
# Example for Windows:
# WORKSHOP_DATA_PATH=E:\Games\Escape from Duckov\Workshop
#
# Example for Linux/Mac:
# WORKSHOP_DATA_PATH=/home/user/games/duckov/workshop
WORKSHOP_DATA_PATH=E:\path\to\your\workshop\folder

# ==========================================
# Database Configuration
# ==========================================
# SQLite database path for storing mod metadata and translations
# Relative paths are resolved from the app root
DB_PATH=./data/mods.db

# ==========================================
# Translation Cache Settings
# ==========================================
# How long to keep cached translations before requiring refresh
TRANSLATION_CACHE_TTL_DAYS=7

# ==========================================
# Removed Configuration (No Longer Needed)
# ==========================================
# The following configuration was removed in the Electron IPC migration:
#
# - STEAM_API_KEY: No longer needed (offline mode, no Steam API calls)
# - STEAM_APP_ID: No longer needed (offline mode)
# - DEEPL_API_KEY: Replaced with offline translation using Transformers.js
# - DEEPL_API_URL: Replaced with offline translation
# - CACHE_TTL_HOURS: No longer needed (no HTTP caching)
# - RATE_LIMIT_*: No longer needed (no HTTP server)

